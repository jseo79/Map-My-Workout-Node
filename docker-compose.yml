version: '2.15.1'
services:
    mysql:
        environment:
            - MYSQL_USER=admin
            - MYSQL_PASSWORD=admin
            - MYSQL_DATABASE=dev_db
            - MYSQL_ROOT_PASSWORD=admin
        image: mysql
        restart: always
        ports:
            - '3306:3306'
